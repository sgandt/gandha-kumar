#include <stdio.h>
int main()
{
int i,n,o,p;
printf("enter the value : ");
scanf("%d",&n);
printf("Enter the power value :");
scanf("%d",&o);
ans=1;
for(i=1;i<=b;i++)
{
	p=p*o;
}
printf("the value is:%d",p);
return 0;
}
